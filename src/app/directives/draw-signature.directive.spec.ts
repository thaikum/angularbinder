import { DrawSignatureDirective } from './draw-signature.directive';

describe('DrawSignatureDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawSignatureDirective();
    expect(directive).toBeTruthy();
  });
});
